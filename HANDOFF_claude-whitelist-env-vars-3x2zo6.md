# HANDOFF — ветка `claude/whitelist-env-vars-3x2zo6`

Append-only журнал сессий (Ц6). Новые записи — в конец.
Предыстория проекта — в `HANDOFF_claude-sorp-group-ai-mvp-40v8h9.md`.

## Сессия 1 — 2026-09-09 — приёмка среды (вайтлист + ключ)

### Контекст
Пользователь: «добавил вайтлист в env, ключ Клод тоже там». Задача сессии — проверить,
что доехало, и записать состояние среды, чтобы спринты не упирались в сеть/ключи.

### Что измерено
- `CLAUDE_KEY` есть и рабочий: `GET api.anthropic.com/v1/models` → 200,
  негативный контроль (неверный ключ) → 401. Доступны `claude-opus-5`, `claude-sonnet-5`,
  `claude-fable-5-1`. Имени `ANTHROPIC_API_KEY` в среде НЕТ — SDK по умолчанию читает его,
  поэтому в коде ключ брать явно из `CLAUDE_KEY`.
- `ANTHROPIC_BASE_URL` = `https://api.anthropic.com` (публичный), подменять не надо.
- Ключей `SERPER_API_KEY`, `SUPABASE_*`, `HUBSPOT_TOKEN` в среде нет — сети хватает,
  ключей нет. Открытый вопрос к пользователю.
- Вайтлист применён, но `*.domain` пропускает **только поддомены**: из 114 доменов ответили
  8 apex, 106 рвутся на прокси (curl exit 56) при живом `www.`. Ломает редиректы
  `www.supabase.com → supabase.com` и т.п.

### Что сделано
- `docs/ops/check_whitelist.sh` — воспроизводимая проверка, три исхода OK/BLOCKED/ORIGIN (Р1),
  с негативным контролем (`example.com` → BLOCKED, `pypi.org` → OK).
- `docs/ops/whitelist_check.txt` — вывод прогона 2026-09-09 (артефакт замера, И2/И3).
- `docs/ops/whitelist.txt` — каждый домен теперь двумя строками: apex + wildcard (228 строк).
- `docs/ops/env_status.md` — состояние ключей и сети с пометками ИЗМЕРЕНО/НЕПРОВЕРЕНО.

### Открыто для следующей сессии
1. Пользователю перезалить обновлённый `whitelist.txt` в настройки среды; после этого
   прогнать `docs/ops/check_whitelist.sh` — apex-строки должны стать `apex=OK`.
   До перезаливки исправление НЕ проверено (И3): правка файла ≠ применённая политика.
2. Нужны ли ключи Serper / Supabase / HubSpot в среде — вопрос к пользователю.
3. Совсем закрыты (и apex, и www): `dubaipulse.gov.ae`, `bayanat.ae`, `economy.ae`,
   `growth.gov.ae`, `mohre.gov.ae`, `supabase.co`, `sorptaxaccounting.com`.
   Первые пять — это источники данных из концепции; если они нужны, политика их не пускает.
4. 403/503 от самих сайтов (антибот, не политика): `bayut.com`, `bayt.com`, `cbre.com`,
   `savills.com`, `dubai.ae`, `uaelegislation.gov.ae`, `arabianbusiness.com`.

## Сессия 1, продолжение — 2026-09-09 — вайтлист перезалит, исправление проверено

- Пользователь залил обновлённый `whitelist.txt` (apex + wildcard) в настройки среды.
  Политика применилась к текущему контейнеру, новая сессия не понадобилась.
- Прогон `docs/ops/check_whitelist.sh`: **apex-хостов, обрубленных политикой, стало 0**
  (было 106). apex: OK 96 / POLICY 0 / ORIGIN 9 / NETFAIL 7 / NODNS 2.
  Пункт 1 из «открыто для следующей сессии» закрыт.
- Проверялка переписана: пять исходов вместо трёх. Оказалось, прокси отдаёт
  `CONNECT tunnel failed, response 403` и на закрытый домен, и на несуществующий, —
  прежняя версия называла бы `www.hubapi.com` заблокированным, хотя у него просто нет
  A-записи. Теперь классы `POLICY` и `NODNS` разделены проверкой DNS, а `NETFAIL`
  (битый TLS у госсайтов ОАЭ) отделён от «закрыто».
- Осталось открытым: ключи Serper / Supabase / HubSpot в среде так и не заданы;
  госисточники `dubaipulse.gov.ae`, `mohre.gov.ae`, `economy.ae`, `moet.gov.ae`,
  `growth.gov.ae` доезжают по сети, но рвут TLS на apex — нужен рабочий поддомен/путь,
  вайтлистом это не чинится.

## Сессия 1, продолжение — 2026-09-09 — ключи

Пользователь добавил ключи. Имена в среде: `CLAUDE_KEY`, `SERPER_KEY`,
`HUBSPOT_PERSONAL_KEY`, `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`,
`SUPABASE_SECRET_KEY`, `SUPABASE_JWKS_URL`. Значения нигде не печатались.

Прогон `docs/ops/check_keys.sh` (живой запрос + негативный контроль на тот же эндпоинт),
вывод — `docs/ops/keys_check.txt`:
- `CLAUDE_KEY` 200 / bogus 401 — рабочий;
- `SERPER_KEY` 200 / bogus 403 — рабочий;
- `SUPABASE_PUBLISHABLE_KEY` и `SUPABASE_SECRET_KEY` 404 «table not found» / bogus 401 —
  рабочие (корень `/rest/v1/` пускает только secret, поэтому проверка идёт запросом к
  несуществующей таблице: 404 = авторизация пройдена);
- `SUPABASE_JWKS_URL` 200;
- **`HUBSPOT_PERSONAL_KEY` не работает**: 401 `EXPIRED_AUTHENTICATION`, «expired 20705
  days ago», expire time 1970-01-01 — то же, что у заведомо неверного токена. В переменной
  лежит OAuth access token (`CiR…`), а нужен Private App token (`pat-naX-…`) со скоупами
  `crm.objects.contacts.*` / `crm.objects.companies.*`. `?hapikey=` тоже 401, HubSpot его
  отключил.

Открыто: заменить HubSpot-токен. Остальное для спринтов 1–3 готово — сеть и три ключа.

## Сессия 1, продолжение — 2026-09-09 — источник заменён, начата разработка

### Продуктовое решение
- Dubai Pulse выброшен: портал не отвечает (см. `docs/data/dubai_pulse_verdict.md`).
- Источник лидогенерации — **GLEIF** (`api.gleif.org`, без аутентификации). Замеры:
  9362 юрлица ОАЭ, 3937 с просроченной регистрацией LEI, ~20-25 новых в месяц.
  Схема и границы — `docs/data/gleif_schema.md`.
- В демо и README прямо заявляется: источник — только компании с LEI, это срез рынка,
  а не весь рынок; источник сменный (адаптер), подключение платного провайдера или
  выгрузки клиента — замена реализации протокола `SourceAdapter`.
- Владелец выбрал: входящие обращения — главный экран, лидогенерация — вторая половина.

### Сделано
- Скелет: `pyproject.toml`, `Makefile`, `.env.example`.
- `leadcentre/`: models (Company, Score, Evidence, InboundMessage, LeadFacts),
  sources/base (протокол адаптера), sources/gleif, engine/rubric (пороги и матрица как
  данные), engine/score (детерминированный tier + инварианты + INVALID), report, cli.
- Прогон `OFFLINE=1 python -m leadcentre.cli discover --limit 8`: HIGH 2, MEDIUM 6,
  нарушений 0, пропущено 0.

### Мультиагентная разработка (запущено параллельно)
| Агент | Владеет файлами | Критерий приёмки |
|---|---|---|
| Тесты | `tests/` | мутация трёх порогов и ячейки матрицы в обе стороны красит тест |
| Извлечение | `engine/extract.py`, `prompts/` | 6 обращений с краёв + негативный контроль на вырезание телефона |
| Ответы | `engine/reply.py`, `engine/lint.py`, `data/pricelist_demo.yaml` | 10 черновиков, мутация прайса красит линтер |
| Данные | `data/inbound_seed.csv`, `eval/labels_template.csv` | 70 обращений, распределение по категориям числами |

Коммиты делаю я один — параллельные агенты в git не пишут (иначе гонка за индекс).

### Открыто
- Ось C (намерение и срочность) в рубрику — после того как тестовый агент вернёт мутации.
- Разметка 30 обращений владельцем — как только готов `eval/labels_template.csv`.
- Токен HubSpot по-прежнему не доезжает в контейнер; CRM пока через заглушку.

## Сессия 2 — 2026-09-09 — `leadcentre/engine/extract.py` + `prompts/extract_v1.md`

### Что сделано
- `prompts/extract_v1.md` — промпт с версией в имени; в коде только загрузка (`load_prompt`).
- `leadcentre/engine/extract.py`: `extract(InboundMessage) -> LeadFacts`,
  `extract_detailed()` (то же плюс время/токены/что вырезано), `scrub_pii()`,
  `build_request_body()`, `ExtractionError`.
  Библиотека — официальный SDK `anthropic` (1.4.0), ключ из `CLAUDE_KEY`
  (запасной `ANTHROPIC_API_KEY`), модель из `LLM_MODEL`, по умолчанию `claude-opus-5`.
  **В `pyproject.toml` зависимость не добавлена — это делает владелец файла.**
- Три исхода (Р1): факты / заглушка OFFLINE (`confidence=0.0`) / `ExtractionError`.
  Пустой `LeadFacts` при ошибке не возвращается.
- ПД вырезаются до отправки; `has_contact` считает код по факту вырезанного (Е2),
  цитаты сличаются с текстом, недословные выбрасываются и считаются числом.

### Что измерено (И3)
- Негативный контроль по телефону: `+971 50 123 45 67` и почта отсутствуют в теле запроса,
  «8 человек» и «2026-09-30» на месте; на входе без ПД скрабер текст не трогает. 7/7 OK.
- Офлайн-проверки (схема = поля `LeadFacts`, разбор ответа, OFFLINE, мутация
  `MIN_PHONE_DIGITS` в обе стороны): проверено 16, нарушений 0.
- `ruff check leadcentre/engine/extract.py` — чисто.

### НЕ ПРОВЕРЕНО — живого вызова модели не было
`POST /v1/messages` отдаёт **400 `Your credit balance is too low to access the Anthropic API`**
на все 6 обращений и на минимальный запрос `{"model":"claude-opus-5","max_tokens":16}` без
моей схемы (req_011CescxBbBQJZeSAm9qJ6q5). Ключ жив: `GET /v1/models` → 200,
заведомо неверный ключ → 401. То есть дело не в коде и не в ключе, а в балансе аккаунта.
Пока баланс не пополнят, качество извлечения (`quotes`, `timeline_days`, `is_spam`)
не измерено ни на одном обращении.

### DEBT
- `# DEBT(2026-09-09)`: фикстуры 6 обращений и проверки лежат в скрэтчпаде сессии,
  а не в `tests/` — каталог пишет другой агент (Ц2). Перенести, когда tests/ освободится.

### Приёмка агентов — 2026-09-09

Проверял сам, не по отчётам агентов (И1: вердикт выносит не тот, кто делал).

- Тесты: `OFFLINE=1 python -m pytest` → **94 passed**, `ruff check leadcentre tests` чист.
  Мутации: 12 по rubric (три порога в обе стороны, четыре ячейки матрицы, целевой город,
  код регистратора) + 2 по правилу выбора названия — покраснели все, ни одна не прошла.
- Данные: пересчитал сам по `data/inbound_seed.csv` — 70 строк, каналы
  jivo 24 / whatsapp 20 / telegram 16 / form 10, языки ru 47 / en 16 / mixed 6 / ar 1,
  длина 0..1332, почт вне example-доменов нет, `eval/labels_template.csv` — 30 строк,
  колонка разметки пустая.
- Ответы: прогнал `draft` + `lint` на срочном обращении — проверено 4, нарушений 0;
  заведомо плохой текст — 6 нарушений. Линтер управляется прайсом, а не совпадением.

Поправка к записи истории: в сообщении коммита 821a3a8 сказано про «известное красное»
в tests/test_gleif.py. На момент коммита тест уже был починен автором, дерево зелёное;
строка в сообщении устарела до того, как коммит был сделан. Исправить сообщение нельзя —
коммит запушен, поэтому поправка живёт здесь.

Найденный продуктовый дефект (отдан в работу автору reply.py): замыкающий вопрос
черновика спрашивал то, что клиент уже написал («сколько человек?» при headcount=8).
Отправлять такое клиенту нельзя — выглядит как непрочитанное обращение.

### Блокирующее
**Инференс Anthropic закрыт балансом аккаунта.** `POST /v1/messages` → 400
`invalid_request_error: Your credit balance is too low`. Проверено на Opus 5 и Haiku 4.5;
`GET /v1/models` тем же ключом → 200, с неверным ключом → 401, то есть ключ рабочий.
Пока баланс не пополнен: извлечение фактов, eval и разметка стоят; скоринг GLEIF,
черновики, линтер и тесты работают без модели.

## Сессия 2, продолжение — 2026-09-09 — extract.py стал провайдер-независимым

### Что сделано
- Внутренний интерфейс `Provider` (протокол: `model()` / `build_body()` / `complete()`)
  и две реализации: `AnthropicProvider` (SDK, structured outputs) и
  `OpenAICompatibleProvider` (Pollinations, `https://text.pollinations.ai/openai`,
  urllib, `Authorization: Bearer $POLLINATIONS_API_KEY`). Выбор — `LLM_PROVIDER`
  (`anthropic` | `pollinations`, по умолчанию anthropic), модель — `LLM_MODEL`,
  по умолчанию `claude-opus-5` / `openai` (алиас GPT-OSS 20B).
  Неизвестный `LLM_PROVIDER` — ошибка, а не тихий откат на дефолт.
- В конвейере (одинаково для всех провайдеров, провайдер это не переопределяет):
  `scrub_pii` до сборки тела, общая валидация `parse_facts`, `has_contact` из скраба.
  У Pollinations structured outputs нет — схема из того же `_schema()` кладётся в
  системный промпт, ответ валидируется тем же `parse_facts` (Е1, не копия).
- Новый тип исхода `ProviderBudgetError(ExtractionError)` — «провайдер недоступен по
  лимитам»: 400 `credit balance` у Anthropic и 402 `KEY_BUDGET_EXHAUSTED` у Pollinations,
  с текстом, что делать. В общую сетевую ошибку не сваливается.
- ИЗМЕРЕНО: без своего `User-Agent` Cloudflare перед Pollinations отдаёт
  403 «error code: 1010» на дефолтный `python-urllib/*`. Заголовок добавлен.

### Что измерено (И3)
- ПД на теле запроса каждого провайдера + выбор провайдера + разбор ответа формата
  OpenAI + 7 видов невалидного ответа: **проверено 42, нарушений 0**.
- Схема = поля LeadFacts, OFFLINE, мутация `MIN_PHONE_DIGITS` в обе стороны:
  **проверено 16, нарушений 0**.
- `ruff check --select E,F,W,I,E501` по extract.py — чисто.

### НЕ ПРОВЕРЕНО — живой генерации по-прежнему не было
- Anthropic: 400 `credit balance is too low` (req_011CeseBhSGhx9rXMvumVUaH).
- Pollinations с ключом: **402 KEY_BUDGET_EXHAUSTED**, «this request costs ~0.0002 pollen,
  but this key has 0.0000», лимит поднимается на enter.pollinations.ai/keys.
- ВАЖНО, чтобы следующая сессия не обманулась: `POST /openai` с повторяющимся промптом
  («say hi») отдаёт **200 из кэша** — один и тот же `id` `pllns_d74d6556…` на всех
  прогонах, с ключом и анонимно. На НОВОМ промпте (nonce в тексте) и анонимно, и с
  ключом — 502/402. То есть 200 на кэше не означает работающую генерацию.
- Качество извлечения (`quotes`, `timeline_days`, `is_spam`) не измерено ни на одном
  обращении ни у одного провайдера. Нужен бюджет: Anthropic Plans & Billing либо
  лимит ключа Pollinations.

### Методологический дефект проверки мутацией — найден и устранён 2026-09-09

Python сверяет кэш `.pyc` по паре (mtime в **целых секундах**, размер файла). Мутация
константы на значение той же длины (`0.5` → `0.1`) размер не меняет, и если две мутации
идут внутри одной секунды, интерпретатор берёт старый байт-код: pytest показывает
результат ПРЕДЫДУЩЕЙ мутации. То есть мутант может выглядеть убитым, не будучи убитым, —
проверка молча меряет не то (ровно то, от чего защищает И5).

Поймано по подозрительно одинаковым спискам падений у пары мутаций `LOW_CONFIDENCE`.
Как теперь гоняются мутации:

    find . -name __pycache__ -type d -exec rm -rf {} +
    OFFLINE=1 PYTHONDONTWRITEBYTECODE=1 python -B -m pytest -p no:cacheprovider

Перепроверено мной независимо: мутация `URGENT_TIMELINE_DAYS` 30 → 60 при таком прогоне
красит `test_urgent_timeline_threshold_is_30_days[31]`, рубрика возвращена байт-в-байт,
контрольный прогон снова 137 зелёных.

Всего по движку убито 20 мутаций из 20: 12 по осям A/B (пороги, ячейки матрицы, целевой
город, код регистратора) и 12 по оси C (четыре порога в обе стороны, базовые уровни,
лестница приоритетов), с пересечением по перепрогону старых.

## Сессия 2, продолжение — 2026-09-09 — живой прогон, выбор модели по времени и цене

### Починено
- `output_config.format.schema` не принимает `minimum`/`maximum` — 400 «For 'integer' type,
  property 'minimum' is not supported». Убрано из схемы, границы переехали в `parse_facts`
  (`HEADCOUNT_MIN=1`, `TIMELINE_MIN=0`), в промпт добавлено словами. Схема целиком
  проверена прогоном: с ней запрос отвечает 200.
- Пустое обращение (edge-01) в модель не отправляется: `ExtractionError` «извлекать нечего».
- Модель, effort и мышление — конфигурация: `LLM_MODEL`, `LLM_EFFORT`, `LLM_THINKING`.
  ИЗМЕРЕНО: Haiku 4.5 отвечает 400 и на `output_config.effort`, и на adaptive-мышление,
  поэтому код шлёт их только моделям из `EFFORT_MODELS`.
- ИЗМЕРЕНО через `GET /v1/models`: на аккаунте есть `claude-haiku-4-5-20251001`;
  имени `claude-haiku-4-5` (без даты) в списке НЕТ.

### Выбор по умолчанию: claude-haiku-4-5-20251001 (ИЗМЕРЕНО на 3 обращениях)
| связка | с/лид | $/лид |
|---|---|---|
| haiku-4.5 | 5.9 | 0.00299 |
| opus-5, effort=low, мышление off | 8.1 | 0.01598 |
Opus дороже в 5.3 раза и медленнее в 1.4. Разница качества найдена на edge-03: Opus
поймал `timeline_days=7` («мы в дубае с 20 числа»), Haiku оставил null и выдал
`jurisdiction_hint='mainland, freezone'` — это вопрос клиента, а не подсказка.
Дорогая модель включается `LLM_MODEL=claude-opus-5 LLM_EFFORT=low`, править код не надо.

### Прогон 8 краевых обращений (haiku-4.5, seed из data/inbound_seed.csv)
Обращений 8, извлечено 7, не смогли 1 (edge-01 — пустой текст, честный исход).
Время 35.1 s (5.0 s на лид), токены in=14260 out=973, стоимость $0.0191 ($0.00273 на лид).
spam-03 → `is_spam=True`. edge-03 → телефон и почта в теле запроса отсутствуют,
`has_contact=True` от скраба.
Потрачено за сессию около $0.09 из $5.

### Открыто
- Качество мерено на 8 обращениях, это мало (Т3): нужен размеченный набор и сверка
  поля с полем, прежде чем считать Haiku достаточным.
- `# DEBT(2026-09-09)`: фикстуры и прогоны — в скрэтчпаде, не в `tests/` (Ц2).

## Сессия 2, продолжение — 2026-09-09 — prompts/extract_v2.md: смысл полей

Дефект (нашёл координатор на живом прогоне): в `budget_hint` попадал ВОПРОС клиента о
цене — prc-01 `'скок стоит'`, acct-01 `'сколько в месяц выйдет'`. Скоринг читает это поле
как «клиент назвал бюджет» и выдавал ложный HIGH.

По И7 греп по форме дефекта («вопрос клиента записан как факт») нашёл второе место:
`jurisdiction_hint` на edge-03 — клиент спрашивает «mainland или freezone?», а модель
клала `'mainland, freezone'`. Починены оба поля, не только названное.

- `prompts/extract_v2.md` — новая версия (v1 оставлен рядом для сравнения),
  `PROMPT_VERSION` в коде переключён. Правила: `budget_hint` только при названном числе
  денег («нет числа — нет budget_hint»), `jurisdiction_hint` `null`, когда клиент
  спрашивает о выборе юрисдикции. Плюс 6 примеров ровно на эти границы.

ИЗМЕРЕНО (haiku-4.5, 3 обращения, v1 → v2):
| id | budget_hint до | budget_hint после |
|---|---|---|
| prc-01 | `'скок стоит'` | `None` |
| acct-01 | `'сколько в месяц выйдет'` | `None` |
| edge-03 | `'ориентируемся на 120-150 тысяч дирхам'` | сохранился |
`jurisdiction_hint` edge-03: `'mainland, freezone'` → `None`.

### Поехало вместе с правкой (не заглажено)
- prc-01 `request_types` `[setup, office]` → `[setup]`, `confidence` 0.4 → 0.3 — лучше.
- acct-01 `confidence` 0.85 → 0.75, цитат 4 → 3.
- Промпт вырос: входных токенов на обращение 1927 → 2934 (+52%), цена лида
  ~$0.0027 → ~$0.0031. Всё ещё в 5 раз дешевле Opus.
- **edge-03 `timeline_days` нестабилен между прогонами: None (v1) / 7 / 38.**
  Правильный ответ 7 («мы в дубае с 20 числа», обращение от 2026-08-13); 38 — ошибка
  модели. Правкой промпта это не вызвано (v1 давал None), но и не вылечено.
  Открыто: либо правило про относительные даты в v3, либо Opus на длинных обращениях.

Потрачено за сессию около $0.11 из $5.
